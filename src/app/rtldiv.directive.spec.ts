import { RTLDivDirective } from './rtldiv.directive';

describe('RTLDivDirective', () => {
  it('should create an instance', () => {
    const directive = new RTLDivDirective();
    expect(directive).toBeTruthy();
  });
});
